 <div class="search-form">
      <%= form_with  url:users_path, method: :get, local: true do |f| %>
        <%= f.text_field :content %>
        <%= f.submit 'search', class: "btn btn-sm btn-outline-secondary" %>
      <% end %>
</div>
